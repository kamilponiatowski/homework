export interface DefaultTheme {
    primary: {
        color: string;
        background: string
        border?: string
    }
    company: {
        color: string
        background: string
        border: string
    }
}